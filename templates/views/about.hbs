<!DOCTYPE html>
<html>
  <head>
    <title>About</title>
    <link rel="icon" href="img/fav.png">
    <link rel="stylesheet" href="css/styles.css" />
  </head>
  <body>
    <div class="main-content">      
      {{>header}}
      <p>This site was created by {{name}}. It uses data from weatherstack and positionstack.</p>
      <img class="portrait" src="{{imageSrc}}" alt="{{altText}}" />
    </div>
    {{>footer}}
  </body>
</html>
